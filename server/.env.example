# TOTP 备份服务端环境变量模板
# 复制为 .env 并修改 API_KEY（务必使用 ≥32 字符的随机串，App 设置页需填写相同值）

# 单用户 API Key（必填）
API_KEY=please-change-me-to-a-long-random-string

# 监听端口（默认 8080）
PORT=8080

# SQLite 文件路径（容器内路径，对应 docker-compose 数据卷）
DB_PATH=/app/data/totp.db
